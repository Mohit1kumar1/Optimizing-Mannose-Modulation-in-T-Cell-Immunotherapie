process {
    container = 't-cell:1.0'
    executor = 'local' // Change to 'awsbatch', 'slurm', etc., for specific environments
    cpus = 2           // Default
    memory = '4 GB'    // Default
}

docker {
    enabled = true
}  // Enable Docker